#include <bits/stdc++.h>
using namespace std;
int m,t,s;
int main(){
    cin>>m>>t>>s;
    if(t==0)//是有可能t=0的
    {
        cout>>0>>endl;
        return 0;
    }
    if(s%t)//如果:s是t的整数倍
        cout<<max(m-s/t,0)<<endl;
    else
        cout<<max(m-s/t-1,0)<<endl;
    return 0;

}
